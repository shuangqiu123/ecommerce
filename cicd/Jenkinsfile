pipeline {
    agent any

    stages {
        stage('build and test project') {
            steps {
                script {
                    echo 'run mvn install'
                    sh './mvnw install'
                }
            }
        }
    }
}