server:
  port: 9200
Spring:
  application:
    name: sq-user
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url:
    username:
    password:
jwt:
  secret:
  expiration: 604800
  tokenHeader: Authorization
  tokenHead: Bearer

eureka:
  client:
    serviceUrl:
      defaultZone: http://sq-eureka1.local/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ipaddress}:${spring.application.name}:${server.port}:@project.version@